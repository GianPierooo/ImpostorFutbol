# Ejemplo de configuración de keystore para producción
# Copia este archivo como "keystore.properties" en android/ y completa los valores
# ⚠️ IMPORTANTE: keystore.properties NO se sube al repositorio (está en .gitignore)
# 
# NOTA: storeFile es relativo a android/app/, así que "release.keystore" busca en android/app/release.keystore

storeFile=release.keystore
storePassword=TU_CONTRASEÑA_KEYSTORE_AQUI
keyAlias=impostor-futbol-key
keyPassword=TU_CONTRASEÑA_KEY_AQUI

